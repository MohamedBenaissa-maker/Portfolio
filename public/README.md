# Public Assets

Place your `photo.jpg` file in this directory.

The photo should be:
- Square format (recommended: 800x800px or larger)
- JPG or PNG format
- Named exactly `photo.jpg`

The profile component will automatically use this image, or fall back to initials if the file is not found.

